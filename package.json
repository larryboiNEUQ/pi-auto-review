{
  "name": "pi-auto-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/pi-permission-system",
    "packages/pi-permission-safe-allow"
  ],
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.79.0",
    "@earendil-works/pi-coding-agent": ">=0.79.0",
    "@earendil-works/pi-tui": ">=0.79.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.81.0",
    "@earendil-works/pi-coding-agent": "0.81.0",
    "@earendil-works/pi-tui": "0.81.0"
  },
  "scripts": {
    "check": "npm run check --workspaces --if-present",
    "test": "npm run verify:bundle && npm run test --workspaces --if-present",
    "verify:bundle": "node scripts/verify-git-bundle.mjs --checkout .",
    "smoke:git": "node scripts/verify-git-bundle.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
