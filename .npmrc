# Pi Git install runs `npm install --omit=dev` and expects host-provided
# @earendil-works/pi-* APIs via the extension loader aliases — do not auto-install
# those peers into this package's node_modules (that re-embeds the full LLM SDK tree).
legacy-peer-deps=true
